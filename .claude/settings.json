{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c 'import sys,json; d=json.load(sys.stdin); print((d.get(\"tool_response\") or {}).get(\"filePath\") or (d.get(\"tool_input\") or {}).get(\"file_path\") or \"\")' | { read -r f; [ -n \"$f\" ] && ./node_modules/.bin/prettier --write --ignore-unknown \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Formatting with Prettier..."
          }
        ]
      }
    ]
  }
}
